apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "infraudit-frontend.fullname" . }}-nginx
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "infraudit-frontend.labels" . | nindent 4 }}
data:
  nginx.conf: |
    {{- .Values.nginxConfig | nindent 4 }}
